FROM dpage/pgadmin4:latest
EXPOSE 80/tcp
EXPOSE 443/tcp
ENTRYPOINT PGADMIN_DEFAULT_EMAIL=`cat $PGADMIN_DEFAULT_EMAIL_FILE` \
    PGADMIN_DEFAULT_PASSWORD=`cat $PGADMIN_DEFAULT_PASSWORD_FILE` \
    /entrypoint.sh

